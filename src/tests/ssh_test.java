import org.junit.Assert;
import org.junit.Test;

/**
 * Created by d.zhukov on 25.03.14.
 */
public class ssh_test extends Assert{

    @Test
    public void Connection() throws Exception {

        /*
        System.out.println("try connect to 192.168.1.68");
        Log_reader lr = new Log_reader("192.168.1.68","root","123456");

        List<record> list = lr.GetLog(Adapter_to_config.getInstance().GetAccessLevel(), Adapter_to_config.getInstance().GetBeginDate(), Adapter_to_config.getInstance().GetEndDate());

        System.out.println("size = " + list.size());

        assertTrue(list.size() > 0);


        System.out.println("try connect to 192.168.1.69");

        //assertFalse((new Log_reader()).Connect("192.168.1.69", "root", "123456"));
        */
    }
}
